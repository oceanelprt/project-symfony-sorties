{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('home') }}" style="padding-left:50px; padding-right:50px">Sortir.com</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav mr-auto">

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <span class="navbar-text"><strong>Bonjour {{ app.user.prenom }} !</strong></span>

                    <li class="nav-item active">
                        <a class="nav-link" aria-current="page" href="{{ path('home') }}">Accueil</a>
                    </li>

                    <li class="nav-item active">
                        <a class="nav-link" aria-current="page" href="{{ path('utilisateur_info', { 'utilisateur': app.user.id }) }}">Mon profil</a>
                    </li>

                    <li class="nav-item active">
                        <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>

                {% else %}

                    <li class="nav-item active">
                        <a class="nav-link" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
                    </li>

                {% endif %}

            </ul>
        </div>
    </nav>
{% endblock %}