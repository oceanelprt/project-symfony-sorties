<form method="post" action="{{ path('sortie_delete', {'sortie': sortie.id}) }}" onsubmit="return confirm('voulez vous vraiment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie.id) }}">
    <button class="btn">Delete</button>
</form>
